#### 本周工作

1. 比赛：最终提交时的结果是编译错误
2. StarryOS：
   1. 修复wait_pid阻塞问题：由于current_task中储存的是被trap打断的任务，导致trap_handler执行时如果yield就会错误的恢复到用户态。解决方案是current_task储存trap_handler，被打断的任务由Axtask的current储存。
   2. 目前情况：busybox发起fork+execve时，子进程无法被调度到。可能与是调度算法有关，后续需要与罗熙进一步讨论
3. vsched2：
   1. 修改Scheduler::sources字段：原本存储用户地址空间的虚拟地址，导致需要在用户态额外调用user_init来初始化sources。修改成储存相对Scheduler的偏移，这样可以在内核态就进行初始化。

#### 下周工作

1. 跑通 StarryOS
