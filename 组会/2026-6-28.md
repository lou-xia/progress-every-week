#### 本周工作

1. 移植StarryOS继续进行，还没有完全跑通，主要的问题包括：

   1. 检查接口实现，符合之前的设计
   2. 修改trap入口向量的逻辑，防止trap上下文丢失寄存器
   3. 栈分配，之前忘记了栈是向下生长的，导致出现很多脏数据和数据丢失问题
   4. clone时子进程会复用父进程的vdso内容，导致初始化失败，通过重新分配物理页面解决
   5. execve时分配新的地址空间，需要再次初始化一次
   6. yield路径寄存器保存有错误
   7. current_task没有合理设置，导致读取任务时读到错误的任务，读到trap_handler

   目前的问题：

   1. 父进程exit后，子进程变成zombie进程。
2. vsched2库方面，与罗熙交流，修改新接口并实现
3. 文档：目前看过了去年一等奖的初赛文档，用AI参考着毕设生成了一份文档，需要修改。

#### 下周工作

1. 写文档
2. 跑测例
3. 跑通 StarryOS
