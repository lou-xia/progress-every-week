## 学习进度

### 20251120-20251126

#### 进展

在本机上成功下载和编译，能跑起来了。

#### 主要的困难

1. 没有编译和允许模块化项目的经历，在`git clone`的时候没有加参数`--recures`，导致浪费很多时间精力。已解决 [x] ：其实文档上有写，但是没仔细看（）
2. rustup 的自动下载时连接不上 GitHub。我本以为改了 WSL 的代理后就不需要改 rustup 了，实测后不行。已解决 [x] ：换源https://mirrors.tuna.tsinghua.edu.cn/help/rustup/
3. StarryOS的代码结构不是很直观，子模块位置不太明确。需要更进一步的学习。（有没有具体的文档？）

### 20251127-20251203

#### 进展

大概弄明白了项目结构，对 arceos 的启动与初始化的过程有了初步了解

#### 主要的困难

1. 文档较少，关于细节的描述很少，只说明了大概框架
2. 需要一个较为细节（最好是中文）的文档，方便阅读和学习

### 20251204-20251210

#### 进展

1. 找到了一个中文文档，https://azure-stars.github.io/Starry-Tutorial-Book/implementation/boot/
2. 对结构有了更为深入了解，大致明白了启动的流程
3. 大概明白了 arceos 的模块化的结构

#### 主要的困难

1. 文档不够完整，有不少地方是空缺的，且文档中很多地方与我 fork 的代码不一致，估计是文档没有按照代码持续更新？
2. 由于 arceos 的模块化的特性，导致阅读具体实现时很难方便的在 vscode 中找到对应的代码（会跳进下载的库函数中，vscode 没有提供在库函数进行搜索的功能，目前的解决方式是去 Github 上搜索）
3. 由于 starry-os 在不断更新，我 fork 的仓库不是 up to date 的，不知道有什么影响

### 20251211-20251223

#### 进展

继续阅读代码。

目前，对代码整体有了比较清楚的认识，但是对实现的细节没有深究。此外，对 starry-os 调用 arceos 的过程还没看完

### 20251231-20260107

#### 进展

继续阅读代码。

大概弄明白了 StarryOS 的任务模型

在 ArceOS 中是以 TaskInner 为核心调度单位，线程和进程概念作为扩展的内容。在 StarryOS 中，对进程线程做了更多的封装，还需要再进一步看看。

#### 主要的困难

1. 各种模块太多了，需要一个更深入的文档

2. percpu 部分好像只能去 GitHub 中查看，没办法在本地转到定义中查看

3. 调试好麻烦，只能用 GNU